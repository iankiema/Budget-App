databases:
  - name: micronaut
    databaseName: micronaut_7ph0
    user: micronaut_7ph0_user

services:
  - type: web
    name: Cizzla
    runtime: ruby
    buildCommand: "./bin/render-build.sh"
    startCommand: "bundle exec puma -C config/puma.rb"
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: micronaut
          property: connectionString
      - key: RAILS_MASTER_KEY
        sync: false